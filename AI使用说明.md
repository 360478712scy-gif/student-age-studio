# 拾光工坊 AI 使用说明

这份说明写给通过 MCP（或命令行）操作「拾光工坊 · 学生时代模组编辑器」的 AI 助手。还没接入的，先看 [AI安装MCP提示词.md](AI安装MCP提示词.md)。连接后也可以调用 `read_guide` 工具随时重读本说明。

## 一、先知道这几件事
- **所有修改都走编辑器自己的保存**：保存前会备份，会检查版本冲突；有问题时返回 `save_warnings`，不会直接写入。
- **能先预览**：写入类工具都支持 `dry_run: true`，只告诉你会改哪些表，不真的写。
- **遇到 `save_warnings`**：先把警告原文告诉用户，用户同意后，把返回的 `warnings` 原样放进 `confirm` 再调用一次。不要自作主张确认。
- **订阅模组只读**：创意工坊订阅的模组（`readOnly: true`）不能改。要改就先用 `create_mod` 的 `copy_from` 复制一份本地副本。
- **编辑器开着时也能用**：但如果用户在编辑器里有未保存的修改，之后保存会提示冲突。动手前提醒用户先在编辑器里保存；改完后请用户在编辑器里重新打开模组查看。
- **不要猜编号**：人物、背景、音乐、表情、地点、物品、条件模板都有工具可查，查到再写。

## 二、基本流程
1. `list_mods` 找到模组。之后 `mod` 参数写编号，或者写唯一的名称。
2. `mod_summary`、`list_events`、`get_event` 了解现状。改已有内容前，一定先读它。
3. 写入。第一次做某类修改时，先 `dry_run` 看看。
4. `check_mod` 检查有没有断开的跳转、不存在的说话人。
5. 告诉用户改了什么（事件编号、对话编号），请用户在编辑器里打开预览。

## 三、游戏里的剧情是怎么组织的
- **事件**（EvtCfg）：一段剧情的入口。主要字段：
  - `type`：事件类型，决定什么时候触发，用 `list_assets(kind="event_types")` 查。
  - `npc`：关联人物。
  - `mapId`：地点，0 为不限。
  - `condition`：触发前提。
  - `effect`：事件效果。
  - `maxcount`：最多发生几次。
  - `rate`：触发概率。
  - `talkId`：首句对话。
- **对话**（TalkCfg）：一句话，通过 `nextTalk` 接下一句。
  - 编号规则：事件号×1000+序号，工具会自动分配。
  - 说话人：旁白是 -1，主角（白雨）是 0，其他人物填人物编号。
- **选项**（OptionCfg）：一句对话后可以出现玩家选项，每个选项跳到一句对话。
  - 编号规则：事件号×100+序号。
- **条件分支**：一句对话可以带判定条件 `check`。显示这句后开始判定，满足走 `nextTalk`，不满足走 `nextTalk2`。
- **条件和效果都是数字数组的列表**：用 `list_commands` 按关键词查模板，按 `template` 和 `parameters` 填。常见的几种：
  - 属性至少：`[4, 1, 属性编号, 最低值]`。
  - 好感不低于：`[7, 9001, 人物编号, 好感值]`。
  - 指定恋人：`[52, 2, 1, 人物编号]`。
  - 增减好感：`[20, 1, 人物编号, 变化量]`。
  - 获得物品：`[60, 1, 物品编号, 数量]`。
  - 增减属性：`[1, 1, 属性编号, 变化量]`。
- **常用事件类型**：

  | 编号 | 类型 |
  |---|---|
  | 1 | 普通事件（可跳过） |
  | 2 | 社交触发 |
  | 20 | 关系任务 |
  | 22 | 恋爱话题：话题名就是事件标题，npc 写恋人 |
  | 110 | 送礼 |
  | 520 | 表白 |
  | 522 | 恋爱社交：和恋人社交时触发，mapId 决定恋人出现在哪；前提里一般写恋人关系、恋爱值、上一章已发生 |

## 四、写一段剧情
新写用 `create_event`，往已有事件里加用 `add_lines`（`after` 指定插在哪句之后）。`lines` 是一句句对话：

```json
[
  {"speaker": "旁白", "text": "放学后，夕阳把走廊染成了橘色。", "background": 202172, "music": "心动足迹"},
  {"speaker": "肖清雅", "text": "那个……你今天，要一个人回家吗？", "enter": "中", "expression": "害羞"},
  {"speaker": "主角", "text": "（她是在等我吗？）", "options": [
    {"text": "「那就一起走吧。」", "effect": [[20, 1, 105, 5]], "lines": [{"speaker": "肖清雅", "text": "嗯！", "expression": "高兴"}]},
    {"text": "「你是不是有话想对我说？」", "lines": [{"speaker": "肖清雅", "text": "才、才没有！", "expression": "窘迫"}]}
  ]},
  {"speaker": "旁白", "text": "回家的路上，风里有桂花的味道。", "background": 203052},
  {"speaker": "肖清雅", "text": "明天也一起走吧。", "enter": "中", "expression": "微笑", "exit": ["肖清雅"]}
]
```

每句可以写的内容：
- **说话人 `speaker`**：人物名、编号、`旁白` 或 `主角`。`displayName` 可以改这句显示的名字，比如「？？？」。
- **站位 `enter`**：写 `左`、`中`、`右`，表示说话人在这句登场或换到该位置。已经站在那里的人物不会重复登场，所以只在第一次出场或换位置时写。
- **表情 `expression`**：说话人的表情，写名称或编号，用 `list_assets(kind="expressions", person=…)` 查。改其他在场人物的表情用 `expressions: {"人物": "表情"}`。
- **退场 `exit`**：写在这句退场的人物。
- **换背景 `background`**：用 `list_assets(kind="backgrounds")` 查，结果附图片路径，能看图的话先看图再选。**换背景会让所有人物退场**（游戏规则），换背景后第一个说话的人要重新写 `enter`。
- **背景音乐 `music`**：从这句开始播放，一直到下一个写了 `music` 的句子。用 `list_assets(kind="music")` 查。
- **音效 `sound`**：这句播放的音效，用 `list_assets(kind="sounds")` 查。
- **原版动作 `actions`**：写成 `[["人物", 动作编号, 参数…]]`。
  - 3001 跳跃、3002 摇晃、3004 水平移动、3005 转身、3006 换服装、3009 气泡表情。
  - 3012 黑影、3013 取消黑影。
  - 3003 放大：参数为倍数，但只有玩家装了 UP 非官方补丁才按倍数生效，否则原版固定放大到 1.1 倍。
- **玩家选项 `options`**：每个选项写 `text`，选后的对话写 `lines`（可以再嵌套选项），也可以写出现条件 `condition` 和效果 `effect`。分支结束后默认接回下一句主线；`rejoin: false` 表示分支结束即事件结束。

两条站位规则：
- 没写任何动作的句子，游戏会让说话人自动登场。
- 写了动作的句子只执行写出的动作。说话人不在场又没有自己的动作时，工具会自动补上登场。

## 五、改已有的剧情
- **`edit_line`**：改一句话，只改传入的项。
  - 文字、说话人、显示名。
  - 演出：`enter`、`expression`、`exit`、`background`、`sound`、`actions`。
  - `cg`：显示 CG，`end` 结束。
  - `next`：跳到哪句，0 表示到此结束。
  - `check` 加 `next_if_failed`：条件分支。
  - `effect`：本句效果。
  - 取消类写法：`enter: "无"`、`expression: ""`、`sound: "无"`。
- **`set_options`**：整体替换一句话后面的玩家选项。
  - 要保留原选项，就带上它的 `id`。
  - 新选项用 `goto` 跳到已有对话，或用 `lines` 新写对话。
  - 传空数组表示去掉选项。
  - 去掉的选项原来指向的对话会变成孤立对话，改完用 `check_mod` 查。
- **`delete_lines`**：删除对话，前后会自动接上。
- **`set_music`**：给一段连续对话设置背景音乐，或清除。
- **事件本身**：`update_event` 改标题、类型、人物、地点、条件、效果、次数；`delete_event` 删除本模组的事件。

## 六、其他内容：人物、物品、目标、短信、动态……
1. `describe_table` 不写表名：列出游戏的全部配置表和中文名。写表名：看每个字段的含义、类型和引用关系。
2. `read_table` 读取记录，默认只列本模组自己的，`include_original` 可以看原版的，照着原版写最稳。
3. `update_rows` 新增或修改：写 `{编号: {字段: 值}}`，只写要改的字段。`delete` 删除本模组的记录。
   - 新编号一般是 7 位、以 1 开头，并且随机一些，避免和别的模组冲突。
   - 短信（PhoneMsgCfg）按短信界面的规则检查：首条由联系人发送、编号以 001 结尾，白雨每次最多两个回复选项。
   - 对话、选项和事件不要用这个工具，请用上面的剧情工具。

常用表：PersonCfg 人物、PersonGrowCfg 人物成长、ItemCfg 物品、BookCfg 书籍、ShopCfg 商店、IntentCfg 目标、PhoneMsgCfg 短信、KZoneContentCfg 企鹅动态、KZoneCommentCfg 动态评论、InteractCfg 闲聊、ActionCfg 行动、GiftEvtCfg 送礼、CGCfg CG、BgCfg 背景、MapCfg 地点。

## 七、事件外对话（送礼、闲聊、小游戏开场、CG 回忆……）
有些对话不属于事件，而是在别的地方播放，比如送某件礼物时、闲聊进度条走完时、小游戏开场或胜负时、从回忆画廊打开 CG 时。
1. `list_assets(kind="dialogue_uses")` 查有哪些用途、各自需要什么参数。
2. `create_external_dialogue` 新建对话夹，写入对话，并在 `uses` 里绑定用途，比如送礼：`{"kind": "gift", "npc": "肖清雅", "item": 5133}`。
3. `list_external_dialogues` 查看已有的对话夹和绑定。
4. 对话夹里的句子同样可以用 `edit_line` 修改。

## 八、素材和模组
- **`import_asset`**：把本机文件导入模组，返回新编号，之后就能在对话里使用。
  - 图片：背景、CG、人物立绘。立绘要写人物（不写则新建人物）、表情编号、服装和学段。
  - 音频：背景音乐、音效。
- **`create_mod`**：新建空模组；写 `copy_from` 则复制一个已有模组。
- **`backup_mod`**：立即完整备份。
- **`check_mod`**：检查 JSON 语法、断开的跳转和选项、不存在的说话人、没有首句的事件、孤立对话。
- **`json_file`**：最后手段，列出、读取或整体替换模组的 JSON 文件。只在其他工具都做不到时才用；写入必须传 `confirm_overwrite: true`，并且先告诉用户。

## 九、写作建议
- 先读一两个原版或本模组的同类事件（`get_event`、`search_lines`），模仿它的语气、节奏和演出密度。
- 一句台词别太长，一屏能读完，20～40 字比较舒服。动作和心理描写交给旁白。
- 人物第一次出场写 `enter`，情绪变化时换 `expression`，换场景时换 `background`。不要每句都写动作。
- 恋爱、关系类剧情记得写好前提：用 `list_commands` 查好感、恋人、上一章已发生等条件，避免剧情在不该出现的时候触发。

## 十、常见错误
- **`找不到名为「…」的人物`**：用 `list_persons` 查准确名字，或直接写编号。昵称（比如「小雅」）不能当人物名。
- **`模组已被其他窗口修改`**：用户在编辑器或游戏里改过。重新读取后再做一次。
- **`save_warnings`**：见第一节，先问用户。
- **`只能删除本模组自己的记录`**：原版记录不能删。可以用 `update_rows` 覆盖成需要的值。
- **修改后编辑器里看不到**：请用户在编辑器里重新打开模组，或切换一次模组。

## 工具一览
| 工具 | 作用 |
|---|---|
| `read_guide` | 读本说明 |
| `list_mods` / `mod_summary` | 模组列表与概况 |
| `list_events` / `get_event` / `get_line` / `search_lines` | 查看剧情 |
| `list_persons` / `list_assets` / `list_commands` | 查人物、素材、事件类型、用途、条件/效果模板 |
| `create_event` / `add_lines` | 新写剧情 |
| `edit_line` / `set_options` / `delete_lines` / `set_music` | 修改剧情 |
| `update_event` / `delete_event` | 修改事件 |
| `describe_table` / `read_table` / `update_rows` | 其他所有配置表 |
| `list_external_dialogues` / `create_external_dialogue` | 事件外对话 |
| `import_asset` / `create_mod` / `backup_mod` | 素材与模组 |
| `check_mod` / `json_file` | 检查与兜底 |
