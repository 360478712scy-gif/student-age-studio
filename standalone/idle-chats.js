/* A deliberately small native InteractCfg editor, sharing the story renderer. */
(()=>{'use strict';const clone=v=>structuredClone(v),UI=StudentAgeCharacterUI,esc=UI.esc;
function create(host,options){
 const S={data:null,rows:{},talks:{},selected:null,line:null,source:'local',saved:'',revision:null,undo:[],redo:[],busy:false,query:'',grade:1};let renderer=null,alive=true,preview=null,previewRenderer=null,modelDoc=null;const assets=new Map();
 const $=s=>host.querySelector(s),snapshot=()=>JSON.stringify({rows:S.rows,talks:S.talks}),readOnly=()=>!!options.project.readOnly||S.source==='reference',dirty=()=>!!S.data&&snapshot()!==S.saved;
 const rows=()=>S.source==='reference'?S.data.table.referenceRows:S.rows,row=()=>rows()?.[S.selected],name=id=>Number(id)===0?'白雨':S.data.refs.PersonCfg[id]?.name||'未选择人物';
 const getTalk=id=>S.talks[id]||S.data.doc.talks[id];
 const chain=()=>{let id=row()?.talkId;const seen=new Set(),out=[];while(id&&!seen.has(id)&&getTalk(id)){seen.add(id);out.push(getTalk(id));id=getTalk(id).nextTalk?.[0];}return out;};
 const notify=()=>options.onState?.({busy:S.busy,dirty:dirty(),readOnly:!!options.project.readOnly,revision:S.revision,canUndo:!!S.undo.length,canRedo:!!S.redo.length});
 const fail=e=>{options.status(e.message||String(e),true);};
 function change(fn){if(readOnly()||S.busy)return;S.undo.push(snapshot());if(S.undo.length>50)S.undo.shift();S.redo=[];fn();notify();}
 function restore(text){Object.assign(S,JSON.parse(text));if(!row())S.selected=Object.keys(rows())[0]||null;S.line=chain()[0]?.id;render();notify();}
 function history(from,to){if(readOnly()||!S[from].length)return;S[to].push(snapshot());restore(S[from].pop());}
 const assetUrl=path=>'/api/asset?'+new URLSearchParams({projectId:options.project.id,path,token:options.token});
 async function load(){S.busy=true;notify();try{const d=await options.api('/api/idle-chats?'+new URLSearchParams({projectId:options.project.id}));if(!alive)return;S.data=d;S.rows=clone(d.table.localRows||{});S.talks={};S.revision=d.revision;S.saved=snapshot();Object.assign(S.rows,clone(d.legacyRows||{}));S.undo=[];S.redo=[];S.selected=rows()[S.selected]?S.selected:Object.keys(rows())[0]||null;S.line=chain()[0]?.id;render();StudentAgePreviewUI.load();}finally{S.busy=false;notify();}}
 async function save(){if(!dirty()||S.busy||options.project.readOnly)return;S.busy=true;notify();try{const before=JSON.parse(S.saved),changed=Object.fromEntries(Object.entries(S.rows).filter(([id,r])=>JSON.stringify(r)!==JSON.stringify(before.rows[id]))),deleted=Object.keys(before.rows).filter(id=>!S.rows[id]);const result=await options.api('/api/idle-chats-save',{projectId:options.project.id,revision:S.revision,rows:changed,talks:S.talks,deleted});S.revision=result.revision;S.data.doc.talks={...S.data.doc.talks,...clone(S.talks)};S.saved=snapshot();options.status('闲聊已保存。');await options.onSaved?.();}finally{S.busy=false;notify();}}
 function newChat(){change(()=>{const id=STUDIO_IDS.allocate('InteractCfg',{...S.data.table.rows,...S.rows});S.rows[id]={id,npc:0,name:'',text:'',map:[],talkId:0,cond:[],effect:[]};S.selected=String(id);S.line=null;});render();}
 function editLine(fn){const current=getTalk(S.line);if(!current)return;change(()=>{S.talks[current.id]=clone(current);fn(S.talks[current.id]);});const label=host.querySelector(`[data-idle-line="${current.id}"] span`);if(label)label.textContent=getTalk(current.id).content||"空白对话";draw();}
 function background(){const place=S.data.refs.MapCfg[row()?.map?.[0]];return Number(S.grade?place?.bg2||place?.bg:place?.bg)||0;}
 function relink(lines){lines.forEach((t,i)=>{S.talks[t.id]={...clone(t),nextTalk:lines[i+1]?[lines[i+1].id]:[],bg:i===0?background():0};});row().talkId=lines[0]?.id||0;}
 function addLine(speaker){if(!row()?.npc)return;change(()=>{const lines=chain(),id=STUDIO_IDS.allocate('TalkCfg',{...S.data.doc.talks,...S.talks},Number(row().id));lines.push({id,content:'',roleIds:[speaker],roles:[],nextTalk:[],nextTalk2:[],option:[],check:[],effect:[],effect2:[],audio:0,bg:0,highlights:[],screenEffect:[]});relink(lines);S.line=id;});render();}
 async function selectPerson(){await STUDIO_ASSET_PICKER.open('portrait',{socialOnly:true,selectPerson:true,getContext:()=>({project:options.project,revision:S.revision,grade:S.grade}),onUse:result=>{const id=Number(result.personId);if(![2,3,4].includes(Number(S.data.refs.PersonCfg[id]?.init?.[0])))throw Error('请选择可社交人物。');change(()=>{const old=row().npc;row().npc=id;row().name=name(id);for(const t of chain()){S.talks[t.id]=clone(t);S.talks[t.id].roleIds=(t.roleIds||[]).map(v=>v===old?id:v);S.talks[t.id].roles=(t.roles||[]).map(r=>Number(r[0])===old?[id,...r.slice(1)]:r);}});render();return true;}});}
 async function selectPlace(){const selected=await UI.choices('闲聊地点',[{id:0,name:'不限地点'},...Object.values(S.data.refs.MapCfg)],{selected:row().map?.[0]||0});if(!selected)return;change(()=>{row().map=selected.id?[selected.id]:[];for(const [i,t] of chain().entries()){S.talks[t.id]=clone(t);S.talks[t.id].bg=i===0?background():0;}});render();}
 async function command(kind){const r=row(),isCondition=kind==='condition',value=isCondition?await StudentAgeConditionLibrary.open({projectId:options.project.id,rows:r.cond||[],templates:S.data.commands.condition,refs:S.data.refs}):await StudentAgeEffects.open({projectId:options.project.id,rows:r.effect||[],templates:S.data.commands.effect,refs:S.data.refs});if(value){change(()=>r[isCondition?'cond':'effect']=value);render();}}
 function model(){const doc=Object.assign(modelDoc??={}, {...S.data.doc,persons:S.data.refs.PersonCfg,backgrounds:S.data.refs.BgCfg,faces:S.data.refs.ModFaceCfg,talks:{},events:{},branchFolders:{}});const list=chain();for(const [index,t]of list.entries()){const r=clone(t);r.roleIds=[Number(r.roleIds?.[0])===0?0:Number(row().npc)];r.roles=(r.roles||[]).filter(a=>[0,Number(row().npc)].includes(Number(a[0]))&&[3000,3009].includes(Number(a[1])));if(!index)r.roles.unshift([0,1002,1,1,0],[Number(row().npc),1002,1,2,0]);r.bg=index===0?background():0;r.screenEffect=[];r.option=[];r.check=[];r.nextTalk2=[];r.nextTalk=list[index+1]?[list[index+1].id]:[];doc.talks[r.id]=r;}return doc;}
 const rendererOptions=()=>({assetUrl,onAssetStatus:messages=>{const n=$('.idle-stage-status');if(n)n.textContent=messages.join('；');},emojiAtlas:'/api/social-emojis?token='+encodeURIComponent(options.token),talkUi:{manifest:'/api/talk-ui?token='+encodeURIComponent(options.token),resource:resource=>'/api/talk-ui?'+new URLSearchParams({resource,token:options.token})},onBeforeInteract:()=>false});
 async function prepare(doc,state,target){for(const role of Object.values(state.roles).filter(r=>r.visible)){const key=[role.id,S.grade,role.face].join(':');if(assets.has(key))continue;assets.set(key,true);try{const person=doc.persons[role.id];if((S.grade?person?.l2d2:person?.l2d)?.length){const result=await options.api('/api/portraits',{projectId:options.project.id,role:role.id,grade:S.grade,cloth:0,face:role.face,gender:1});if(!alive)return;if(result.frame)StudentAgeScene.portraitFrames.set(`${role.id}-${S.grade}-0`,result.frame);assets.set(key,result);}const paths=StudentAgeScene.portraitCandidates(doc,role).filter(p=>!p.startsWith('portrait-cache/'));if(paths.length){const sizes=await options.api('/api/portrait-dimensions',{projectId:options.project.id,paths});for(const [p,size]of Object.entries(sizes))StudentAgeScene.portraitSizes.set(p,size);}if(alive){target.refreshPortraits();renderFaces();}}catch{assets.delete(key);}}}
 function draw(animate=false,target=renderer,line=S.line){if(!target||!row())return;const doc=model(),trace=chain().slice(0,Math.max(0,chain().findIndex(t=>t.id===Number(line)))+1).map(t=>t.id),state=StudentAgeScene.reconstruct(doc,line,{roots:[row().talkId],trace,grade:S.grade});target.draw(doc,state,{animate});prepare(doc,state,target);}
 function renderFaces(){const pane=$('[data-idle-faces]');if(!pane)return;const t=getTalk(S.line),role=Number(t?.roleIds?.[0])===0?0:Number(row()?.npc),choices=StudentAgeExpressions.choices({person:S.data.refs.PersonCfg[role],faces:S.data.refs.ModFaceCfg,roleId:role,grade:S.grade,cloth:0,metadata:assets.get([role,S.grade,(t?.roles||[]).find(r=>r[0]===role&&r[1]===3000)?.[2]||0].join(':'))});pane.innerHTML=choices.map(f=>`<button data-idle-face="${f.id}" ${readOnly()||!t?'disabled':''}>${esc(f.name)}</button>`).join('');}
 function render(){renderer?.dispose();renderer=null;const current=row(),list=current?chain():[];if(!list.some(t=>t.id===Number(S.line)))S.line=list[0]?.id;const t=getTalk(S.line);host.innerHTML=`<div class="idle-shell"><aside class="idle-index"><nav><button data-idle-source="local" class="${S.source==='local'?'active':''}">当前模组</button><button data-idle-source="reference" class="${S.source==='reference'?'active':''}">游戏参考库</button></nav><input data-idle-search placeholder="搜索闲聊或人物" value="${esc(S.query)}"><div class="idle-tools"><button data-idle="new" ${readOnly()?'disabled':''}>＋ 添加闲聊</button><button data-idle="delete" ${readOnly()||!current?'disabled':''}>删除</button></div><div class="idle-records">${Object.values(rows()).filter(r=>StudentAgeSearch.matches(S.query,r.text,name(r.npc))).map(r=>`<button data-idle-record="${r.id}" class="${String(r.id)===String(S.selected)?'active':''}"><strong>${esc(r.text||'未填写进度文字')}</strong><small>${esc(name(r.npc))}</small></button>`).join('')}</div></aside><main class="idle-main">${current?`<div class="idle-settings"><label>闲聊进度文字<input data-idle-text placeholder="正在和谁……" value="${esc(current.text)}" ${readOnly()?'disabled':''}></label><div class="idle-tools"><button data-idle="person" ${readOnly()?'disabled':''}>${esc(name(current.npc))} ▾</button><button data-idle="place" ${readOnly()?'disabled':''}>${esc((current.map||[]).map(id=>S.data.refs.MapCfg[id]?.name||id).join('、')||'不限地点')} ▾</button><button data-idle="condition" ${readOnly()?'disabled':''}>条件 · ${current.cond?.length||0}</button><button data-idle="effect" ${readOnly()?'disabled':''}>效果 · ${current.effect?.length||0}</button>${S.source==='reference'?'<button data-idle="copy">复制到模组</button>':''}</div></div><div class="idle-edit"><section class="idle-lines"><div class="idle-tools"><button data-idle="add-self" ${readOnly()||!current.npc?'disabled':''}>＋ 白雨</button><button data-idle="add-person" ${readOnly()||!current.npc?'disabled':''}>＋ ${esc(name(current.npc))}</button></div>${list.map((line,i)=>`<button data-idle-line="${line.id}" class="${Number(S.line)===line.id?'active':''}"><small>${i+1} · ${esc(name(line.roleIds?.[0]))}</small><span>${esc(line.content||'空白对话')}</span></button>`).join('')}</section><section class="idle-stage-column"><div class="idle-stage"></div><p class="idle-stage-status" role="status"></p><div class="idle-tools"><button data-idle="preview" ${list.length?'':'disabled'}>▶ 从所选对话预览</button><select data-idle-grade><option value="0" ${S.grade===0?'selected':''}>小学</option><option value="1" ${S.grade===1?'selected':''}>中学</option></select></div>${t?`<textarea data-idle-content rows="3" placeholder="对话内容" ${readOnly()?'disabled':''}>${esc(t.content)}</textarea><div class="idle-tools"><button data-idle="up" ${readOnly()?'disabled':''}>上移</button><button data-idle="down" ${readOnly()?'disabled':''}>下移</button><button data-idle="remove-line" ${readOnly()?'disabled':''}>删除此句</button></div>`:''}</section><aside class="idle-expression"><h3>人物表情</h3><div data-idle-faces></div><h3>气泡表情</h3><button data-idle="emoji" ${readOnly()||!t?'disabled':''}>选择表情</button></aside></div>`:'<p class="idle-empty">添加闲聊，先填写进度文字并选择可社交人物。</p>'}</main></div>`;if(current){renderer=new StudentAgeScene.Renderer($('.idle-stage'),rendererOptions());draw();renderFaces();}notify();}
 async function pickEmoji(){return new Promise(resolve=>{const d=document.createElement('dialog');d.className='social-dialog social-emoji-dialog';const end=v=>{d.close();d.remove();resolve(v);};d.innerHTML='<header><h2>气泡表情</h2><button data-clear>清除</button><button data-close>关闭</button></header><div class="social-emoji-grid">'+(StudentAgeSocialMedia.glyphs||[]).map((g,i)=>`<button data-emoji="${i}" title="表情 ${i}">${StudentAgeSocialMedia.renderText('<sprite='+i+'>','/api/social-emojis?token='+encodeURIComponent(options.token))}</button>`).join('')+'</div>';d.onclick=e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.emoji!==undefined)end({id:Number(b.dataset.emoji)});if(b.hasAttribute('data-clear'))end({id:-1});if(b.hasAttribute('data-close'))end(null);};d.oncancel=e=>{e.preventDefault();end(null);};document.body.append(d);d.showModal();});}
 function openPreview(){if(!chain().length)return;preview?.close();preview=document.createElement('dialog');preview.className='idle-preview';preview.innerHTML='<div class="idle-stage"></div><footer><button data-prev>上一句</button><button data-next>下一句</button><button data-close>返回编辑</button></footer>';document.body.append(preview);preview.showModal();previewRenderer=new StudentAgeScene.Renderer(preview.querySelector('.idle-stage'),rendererOptions());let index=Math.max(0,chain().findIndex(t=>t.id===Number(S.line)));const paint=()=>{draw(true,previewRenderer,chain()[index].id);preview.querySelector('[data-prev]').disabled=!index;preview.querySelector('[data-next]').disabled=index===chain().length-1;};preview.querySelector('[data-prev]').onclick=()=>{index--;paint();};preview.querySelector('[data-next]').onclick=()=>{index++;paint();};preview.querySelector('[data-close]').onclick=()=>preview.close();preview.onclose=()=>{previewRenderer?.dispose();previewRenderer=null;preview.remove();preview=null;};paint();}
 host.oninput=e=>{if(e.target.matches('[data-idle-search]')){S.query=e.target.value;const pos=e.target.selectionStart;render();$('[data-idle-search]').focus();$('[data-idle-search]').setSelectionRange(pos,pos);}if(e.target.matches('[data-idle-text]')){change(()=>row().text=e.target.value);const label=host.querySelector(`[data-idle-record="${S.selected}"] strong`);if(label)label.textContent=row().text||'未填写进度文字';}if(e.target.matches('[data-idle-content]'))editLine(t=>t.content=e.target.value);};
 host.onchange=e=>{if(e.target.matches('[data-idle-grade]')){S.grade=Number(e.target.value);draw();renderFaces();}};
 host.onclick=async e=>{const b=e.target.closest('button');if(!b||b.disabled)return;try{if(b.dataset.idleSource){S.source=b.dataset.idleSource;S.selected=Object.keys(rows())[0];S.line=null;render();return;}if(b.dataset.idleRecord){S.selected=b.dataset.idleRecord;S.line=null;render();return;}if(b.dataset.idleLine){S.line=Number(b.dataset.idleLine);render();return;}if(b.dataset.idleFace){editLine(t=>{const role=Number(t.roleIds?.[0])===0?0:Number(row().npc);t.roles=(t.roles||[]).filter(r=>!(r[0]===role&&r[1]===3000));t.roles.push([role,3000,Number(b.dataset.idleFace)]);});renderFaces();return;}const a=b.dataset.idle;if(a==='new')newChat();if(a==='delete'){change(()=>{delete S.rows[S.selected];S.selected=Object.keys(S.rows)[0];S.line=null;});render();}if(a==='person')await selectPerson();if(a==='place')await selectPlace();if(['condition','effect'].includes(a))await command(a);if(a==='add-self')addLine(0);if(a==='add-person')addLine(Number(row().npc));if(a==='preview')openPreview();if(a==='copy'){const source=clone(row()),lines=chain().map(clone);S.source='local';change(()=>{const id=STUDIO_IDS.allocate('InteractCfg',{...S.data.table.rows,...S.rows});source.id=id;S.rows[id]=source;S.selected=String(id);for(const line of lines){line.nextTalk2=[];line.option=[];line.check=[];line.id=STUDIO_IDS.allocate('TalkCfg',{...S.data.doc.talks,...S.talks},Number(row().id));S.talks[line.id]=line;}relink(lines);S.line=lines[0]?.id;});render();}if(['up','down','remove-line'].includes(a)){change(()=>{const list=chain(),i=list.findIndex(t=>t.id===Number(S.line));if(a==='remove-line'){list.splice(i,1);S.line=list[Math.min(i,list.length-1)]?.id;}else{const j=i+(a==='up'?-1:1);if(j>=0&&j<list.length)[list[i],list[j]]=[list[j],list[i]];}relink(list);});render();}if(a==='emoji'){const result=await pickEmoji();if(result)editLine(t=>{const role=Number(t.roleIds?.[0])===0?0:Number(row().npc);t.roles=(t.roles||[]).filter(r=>!(r[0]===role&&r[1]===3009));if(result.id>=0)t.roles.push([role,3009,result.id,0]);});}}catch(error){fail(error);}};
 return {jsonRead:(name,disk={})=>{const path={"InteractCfg": ["rows"], "TalkCfg": ["talks"]}[name];if(!path||!S.data)return null;return name==='TalkCfg'?{...disk,...S.talks}:path.reduce((v,k)=>v?.[k],JSON.parse(snapshot()));},
 jsonWrite(name,rows){const path={"InteractCfg": ["rows"], "TalkCfg": ["talks"]}[name];if(!path)return false;if(readOnly()||S.busy)throw Error('当前页面暂不可编辑。');const data=JSON.parse(snapshot());let parent=data;for(const k of path.slice(0,-1))parent=parent[k];if(JSON.stringify(parent[path.at(-1)])===JSON.stringify(rows))return true;S.undo.push(snapshot());if(S.undo.length>60)S.undo.shift();S.redo=[];parent[path.at(-1)]=structuredClone(rows);restore(JSON.stringify(data));notify();return true;},
 jsonSaved(name,rows,revision){const path={"InteractCfg": ["rows"], "TalkCfg": ["talks"]}[name];if(!path)return;const data=JSON.parse(S.saved);let parent=data;for(const k of path.slice(0,-1))parent=parent[k];parent[path.at(-1)]=structuredClone(rows);S.saved=JSON.stringify(data);S.revision=revision;notify();},load,save,snapshot,dirty,undo:()=>history('undo','redo'),redo:()=>history('redo','undo'),discard:()=>restore(S.saved),destroy(){alive=false;renderer?.dispose();preview?.close();host.innerHTML='';},focusSearch:()=>$('[data-idle-search]')?.focus(),navigation:{capture:()=>({selected:S.selected,line:S.line,source:S.source}),restore:v=>{Object.assign(S,v);render();}}};
}
window.StudentAgeIdleChats={create};})();
