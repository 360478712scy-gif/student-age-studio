# 拾光工坊 1.3.15

- 打开事件时，会把连到这个事件上的事件外对话，以及编号落在这个事件段里的对话一起列出来。它们仍留在「事件外对话」里，不会改成这个事件的正式对话；删除事件也不会把它们删掉。从事件入口一路连下去的句子，仍属于这个事件。
- 效果目录新增五条：属性百分比加成、社交时增减属性、社交时额外好感、送礼好感百分比、请教同学的消耗和效果百分比。
- JSON 编码的文件下拉可以搜索，选择菜单不再被编码面板挡住。
- 删除本地模组仍会移到可恢复目录。订阅的工坊模组第二次确认后只从本机列表拿掉，不删除 Steam 里的文件，可以在工坊设置里重新读取。
- 全屏预览按原版 2560×1440 居中，窗口不是 16:9 时人物不再相对背景错位。
- 更新说明：本次是编辑器代码更新，不修改模组、存档与游戏文件。

验证：事件归属的定向测试通过。三平台检查由发布流程执行。本轮未改人物重叠的预览，也未重打完整客户端。
