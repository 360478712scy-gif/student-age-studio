# 拾光工坊 1.3.7.3

优化大模组编辑时逐字输入的卡顿，减少对整份模组的重复比较；文字与舞台预览仍即时更新，立即保存和保存中继续输入均保留草稿。

对话采用完整只读底稿、按 ID 解析及独立修改记录，撤销历史共享底稿，减少重复复制。保存仅提交明确修改和删除的记录；仍按原版 TalkCfg.json 格式合并、备份并原子替换，保留外部修改冲突保护。首次打开和后端写出仍处理完整文件，不是单事件文件分页。

隔离检查覆盖共享对话、改号、空事件、未知字段、嵌套动作、旧引用、撤销重做、中文输入、保存中继续打字、外部修改和失败回滚。21 万字合成模组连续输入的同步处理约从每字 5–6ms 降至 0.2–0.5ms；首次输入仍有元数据快照开销。合成测试不代表所有电脑，真实用户模组及 Windows 原生界面仍需反馈验证。

本次为轻量在线更新，下载完成后保存草稿并重启生效。无需重新下载完整客户端。
