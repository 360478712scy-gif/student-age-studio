# 1.3.7.7

剧情编辑改为后台按需读取对话正文。列表、搜索、人物预览、撤销重做和保存的操作方式保持原样；支持原有中文、拼音和首字母搜索。大型事件切换时只读取所需记录，未修改正文可以回收，未保存草稿独立保留。

事件设置中的“从哪句开始”在没有指定入口时默认使用本事件第一句，仍可手动选择其他句；空事件添加第一句后自动绑定。已有手动选择和男女主角入口保留。

只格式化编辑器自己的缓存，不对模组原文件做分段格式化。首次建索引仍需完整扫描；保存继续使用原来的完整校验、备份、冲突保护和事务写回。原始 JSON、完整导出和改号按需使用完整数据，未加载的对话不会被当成删除。不支持的源格式自动使用原有读取方式。

已做隔离模组的快速切换、连续输入、保存中继续输入、撤销重做、共享对话、素材导入、原始 JSON、导出和改号检查。单事件约 23 万字的 200 次切换保留内存稳定；真实旧模组 2,963 条对话逐字段对照一致。Mac 与 Windows 原生窗口关键操作已验证。这些结果不代表所有模组、电脑或游戏流程均已验收。

本次为轻量在线更新，保存草稿后按原方式重启生效。
